baseURL = 'https://oungin04-math.github.io/'
languageCode = 'ko-kr'
title = 'Praise W'
theme = 'PaperMod'

[params]
  defaultTheme = "auto"
  # 수학 수식(LaTeX) 활성화 설정
  math = true

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true